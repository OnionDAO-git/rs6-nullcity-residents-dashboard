{
  "name": "@nullcity-dashboard/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nullcity-dashboard/shared": "*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.16",
    "@types/ws": "^8.5.13",
    "typescript": "^5.7.3"
  }
}
