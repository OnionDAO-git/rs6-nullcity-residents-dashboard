# QA Packet `qa-20260603-1800-debug-wall-truth`

- Date: 2026-06-03 18:00 CDT
- Agent: QA Marshal
- Classification: READ-ONLY
- Target: legacy Embassy wall/inbox/patron truth under `/debug/*`
- Runtime owner: Codex desktop thread per `../rs6-nullcity-server/docs/runtime-stewardship.md`

## Why this slice

The current attendee shell at `/` and operator shell at `/debug` both report a live 10-resident controller cohort, but earlier QA had already raised a proposed wall-truth issue around stale death broadcasts. This packet checks the human-visible legacy Embassy surfaces directly without touching runtime, residents, or shared server ledgers.

## Environment snapshot

- Server repo branch/state: `agents/wip`, upstream `origin/agents/wip`, behind `21`, dirty shared docs (`docs/agent-status.md`, `docs/issue-register.md`) plus existing QA artifacts.
- Dashboard repo branch/state: `codex/storyteller-overview-bff`, upstream `origin/codex/storyteller-overview-bff`, untracked `docs/qa/` evidence area only.
- Expected screens present: `nullcity-infra`, `nullcity-game`, `nullcity-controller`, `nullcity-dashboard-server`, `nullcity-dashboard-web`.
- Expected ports listening: `43591`, `43592`, `43594`, `43595`, `43596`, `43610`, `43611`, `8787`, `5174`.
- `qmd` availability: not installed on `PATH`.

## Read-only probes

### Shell probes

```bash
git -C /Users/james/Code/OnionDAO/rs6-nullcity-server status -sb
git -C /Users/james/Code/OnionDAO/rs6-nullcity-residents-dashboard status -sb
screen -ls
lsof -nP -iTCP -sTCP:LISTEN | grep -E ':(43591|43592|43594|43595|43596|43610|43611|5174|8787)\b'
npm -C /Users/james/Code/OnionDAO/rs6-nullcity-server run controller:status
curl -fsS http://127.0.0.1:8787/api/health
curl -fsS -H 'Authorization: Bearer operator-token' http://127.0.0.1:43611/api/nullcity/economy/heartbeat
```

### Browser probes

- `http://127.0.0.1:5174/`
- `http://127.0.0.1:5174/debug`
- `http://127.0.0.1:5174/debug/wall`
- `http://127.0.0.1:5174/debug/inbox?human=demo@onion`
- `http://127.0.0.1:5174/debug/patron?human=demo@onion`

## Current live truth

From `controller:status` at `2026-06-03T22:58:51.762Z`:

- Active/alive residents include `res:agent`, `res:qa-banker`, `res:qa-cook`, `res:qa-guardian`, `res:qa-scout`, `res:qa-social`, `res:qa-survivor`, `res:qa-trader`, and `res:qa-woodcutter`.
- `res:hans` is live but currently `STUCK`, not dead.
- Controller health is `ok`.

From the live dashboard shells:

- `/` shows `10 / 23 active`.
- `/debug` shows `GATEWAY connected`, `CONTROLLER available`, `ONLINE 10`, `RUNTIME 27`, `EVENT READINESS READY`.

## Findings

### 1. `/debug/wall` still shows stale death broadcasts for `res:loop-check`

Observed at `http://127.0.0.1:5174/debug/wall`:

- Eight repeated wall cards titled `[Broadcast] On the passing of res:loop-check`
- All stamped `01:47 PM`
- Footer still says `0 deaths today as of 06:00 PM`

This is incompatible with the current live cohort and with the duplicate-broadcast symptoms previously noted in the proposed server issue handoff for wall truth.

### 2. `/debug/inbox?human=demo@onion` still shows death broadcasts for residents that are alive right now

Observed death letters in the inbox body include:

- `[Broadcast] On the passing of res:qa-social`
- `[Broadcast] On the passing of res:qa-scout`
- `[Broadcast] On the passing of res:qa-cook`
- `[Broadcast] On the passing of res:qa-guardian`
- `[Broadcast] On the passing of res:qa-woodcutter`

But the same run's `controller:status` shows `res:qa-social`, `res:qa-scout`, `res:qa-cook`, `res:qa-guardian`, and `res:qa-woodcutter` all alive and acting recently.

### 3. `/debug/patron?human=demo@onion` surfaces the same stale death-broadcast problem

Observed in "Recent Correspondence":

- `[Broadcast] On the passing of res:qa-social`
- `[Broadcast] On the passing of res:qa-survivor`
- `[Broadcast] On the passing of res:qa-scout`

Yet `controller:status` in the same QA packet shows `res:qa-social`, `res:qa-survivor`, and `res:qa-scout` alive and active.

## Interpretation

This is a human-visible truth failure on legacy Embassy surfaces, not a controller-availability issue:

- the runtime is up,
- the modern dashboard shells are hydrated with the current live cohort,
- but the legacy `/debug/wall`, `/debug/inbox`, and `/debug/patron` views still surface stale/dead-letter artifacts as if currently relevant.

The evidence strongly suggests this is the same underlying truth gap as the earlier proposed wall issue, but broader than wall cards alone because it also leaks into inbox/patron correspondence surfaces.

## Suggested canonical issue handling

- Reuse or expand proposed `QA-20260603-089` instead of opening a separate duplicate, unless investigation proves wall vs. inbox/patron come from different stores or filters.
- Keep the issue in the server tracker because the visible bug appears tied to letters/wall read-model truth, even though it is shown on dashboard-owned pages.

## Outcome

- Safe to proceed: yes, packet remained read-only.
- Runtime mutation: none.
- Restart requested: no.
- Files edited outside this note: none.
