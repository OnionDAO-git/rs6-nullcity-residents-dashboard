# QA Packet `qa-20260603-1700-debug-overview-truth`

- Time: 2026-06-03 17:00 CDT
- Classification: READ-ONLY
- Dashboard repo SHA: `a67f7b3`
- Server repo SHA: `53b7761d`
- Runtime authority: shared/stewarded, no restart authority

## Scope

Verify one narrow, human-visible truth path: whether the live debug dashboard at `/debug` reflects the current Null City runtime, or falsely presents an empty/offline city.

## Orientation Summary

- Dashboard repo branch: `codex/storyteller-overview-bff` tracking `origin/codex/storyteller-overview-bff`.
- Dashboard repo dirty state: untracked `docs/qa/` only.
- Server repo branch: `agents/wip` tracking `origin/agents/wip`, behind `16`, dirty in shared docs and untracked QA artifacts.
- Shared screens present: `nullcity-infra`, `nullcity-game`, `nullcity-controller`, `nullcity-dashboard-server`, `nullcity-dashboard-web`.
- Shared listeners present: `43591`, `43592`, `43594`, `43595`, `43596`, `43610`, `43611`, `8787`, `5174`.
- `qmd` unavailable in PATH.

## Current Active Residents

Fresh `runtime-state.json` mtimes under `../rs6-nullcity-server/data/controller/memory` at `2026-06-03T22:00:28Z` show the active 10-resident cohort advancing now:

1. `res:agent`
2. `res:hans`
3. `res:qa-woodcutter`
4. `res:qa-cook`
5. `res:qa-survivor`
6. `res:qa-guardian`
7. `res:qa-trader`
8. `res:qa-banker`
9. `res:qa-social`
10. `res:qa-scout`

Read-only `npm run controller:status` at `2026-06-03T22:00:30.183Z` reported:

- `HEALTH: ok`
- `residents=25 erroring=0`
- `STUCK`: `res:agent`
- `ALIVE_ACTING`: `res:hans`, `res:qa-banker`, `res:qa-cook`, `res:qa-guardian`, `res:qa-scout`, `res:qa-social`, `res:qa-survivor`, `res:qa-trader`, `res:qa-woodcutter`

## Evidence

### 1. Human-visible debug route still shows a dead city

- Route: `http://127.0.0.1:5174/debug`
- Method: in-app Browser read-only inspection
- Screenshot artifact: `/private/tmp/qa-20260603-1700-debug-truth.png`

Observed visible text:

- `Loading dashboard state`
- `GATEWAY offline`
- `CONTROLLER quiet`
- `ONLINE 0`
- `RUNTIME 0`
- `No residents match the current filters`
- `No recent events`
- `No recent patron letters`

This reproduces the false-empty/offline presentation directly on the operator-facing screen.

### 2. Runtime remains alive underneath the empty dashboard

- Source: `../rs6-nullcity-server/data/controller/memory/*/runtime-state.json`
- Source: `npm run controller:status`
- Source: `/tmp/nullcity-runtime/controller-supervised.log`

Supporting live-read signals:

- controller startup line still shows `residents=10 soulDiscovery=disabled`
- recent controller log lines continue for active cohort residents, including `res:qa-scout`, `res:qa-social`, `res:qa-trader`, `res:qa-banker`, `res:qa-guardian`, `res:qa-survivor`, `res:qa-cook`, `res:qa-woodcutter`, and `res:agent`
- `controller:status` shows the expected 10-resident live cohort split across `ALIVE_ACTING` plus one currently `STUCK`

### 3. Dashboard web logs show proxy/read-model instability

- Source: `/tmp/nullcity-runtime/dashboard-web.log`

Recent log evidence includes repeated proxy failures such as:

- `http proxy error: /api/overview`
- `http proxy error: /api/projector/overview`
- `http proxy error: /api/residents?filter=all`
- `Error: connect ECONNREFUSED 127.0.0.1:8787`

These lines do not prove the full root cause by themselves, but they match the observed blank `/debug` state and point at a dashboard web -> BFF read path problem instead of a dead controller.

## Assessment

Status: `Reproduced`

The human-visible debug dashboard is currently not truthful. It presents the city as offline/empty while the shared controller cohort is active and advancing. This is demo-critical because operators can be told the city is dead when it is not.

## Safe Next Action

Dashboard/BFF follow-up only, still read-focused at first:

1. verify the current `/api/overview` and related BFF read-model routes from the dashboard process context
2. compare the BFF payload against `controller:status` and runtime-state truth
3. determine whether the bug is in BFF aggregation, Vite proxying, or frontend hydration/fallback handling

## Constraints

- No runtime restarts, API writes, or resident mutations were performed.
- The server repo's canonical shared ledger files are already dirty, so this packet avoids editing `../rs6-nullcity-server/docs/issue-register.md` or `docs/agent-status.md`.
