# QA Packet `qa-20260603-1554-economy-active-truth`

- Time: 2026-06-03 15:54 CDT
- Classification: READ-ONLY
- Scope: dashboard economy truth vs live resident activity

## Orientation Summary

- Server repo branch: `agents/wip` (`origin/agents/wip`, behind 13), dirty in `docs/agent-status.md`, `docs/issue-register.md`, and multiple untracked QA artifacts.
- Dashboard repo branch: `codex/storyteller-overview-bff` (`origin/codex/storyteller-overview-bff`), dirty with untracked `docs/qa/`.
- Shared runtime screens present: `nullcity-infra`, `nullcity-game`, `nullcity-controller`, `nullcity-dashboard-server`, `nullcity-dashboard-web`.
- Listening ports present: `43591`, `43592`, `43594`, `43595`, `43596`, `43610`, `43611`, `8787`, `5174`.
- `qmd` command unavailable in this shell (`command -v qmd` returned no path).

## Current Active Residents

Fresh `runtime-state.json` mtimes in `../rs6-nullcity-server/data/controller/memory` show 10 active cohort residents at `2026-06-03T20:53:18Z`:

1. `res:agent`
2. `res:hans`
3. `res:qa-woodcutter`
4. `res:qa-cook`
5. `res:qa-survivor`
6. `res:qa-guardian`
7. `res:qa-trader`
8. `res:qa-banker`
9. `res:qa-social`
10. `res:qa-scout`

`inference-health.json` is fresh and healthy:

- `status: ok`
- `model: qwopus3.5-27b-v3@q4_k_s`
- `generatedAt: 2026-06-03T20:53:08.541Z`

## Evidence

### Human-facing overview is live

Route: `http://127.0.0.1:5174/`

Observed after 5s settle in the in-app browser:

- Monday demo path says `10 / 23 active`
- Overview says `10 RESIDENTS ONLINE`
- Story section shows dispatch `live-20260603204913`

### Economy view contradicts active-controller truth

Route: `http://127.0.0.1:5174/economy`

Observed after 3s settle in the in-app browser:

- `CITY TOTALS` shows `31 residents carrying 599,138.5 AP`
- `0 RESIDENTS WITH ECONOMY EVENTS IN 15M`
- `ACTIVE 0`
- `CONTROLLER HEARTBEAT` simultaneously shows `10 / 31 residents active`
- Top resident cards are labeled `ONLINE, NO AP/GP EVENTS`

Interpretation: the economy surface is mixing a live controller heartbeat with a stale or incompatible activity counter for the same window, so the page under-reports active residents even while showing online residents and fresh controller activity.

### Debug legacy surfaces still render

- `http://127.0.0.1:5174/debug/wall` renders resident cards and recent wall content.
- `http://127.0.0.1:5174/debug/inbox?human=demo@onion` renders inbox letters.

## Conclusion

This cycle confirms a current dashboard truth problem on the economy page: resident activity is shown as zero on the main economy counters while the same stack reports 10 active controller-held residents elsewhere. This is narrow, human-visible, demo-relevant, and safe to hand to a dashboard dev agent.

## Recommended Next Action

Claim a dashboard packet to inspect the `packages/server` economy read-model path and align `ACTIVE`/window activity on `/economy` with the same live controller truth used by the overview and heartbeat surfaces.
