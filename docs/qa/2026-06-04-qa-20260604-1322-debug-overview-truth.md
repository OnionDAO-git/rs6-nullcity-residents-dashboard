# QA Packet `qa-20260604-1322-debug-overview-truth`

- Time: 2026-06-04 13:22-13:25 CDT
- Classification: READ-ONLY
- Scope: Live `/debug` overview truth versus current controller cohort truth
- Dashboard repo SHA: `4542463`
- Server repo SHA: `53b7761d`
- Runtime authority: shared/stewarded, no restart authority

## Why This Packet

Prior packets showed a first-paint honesty problem on the operator dashboard. This cycle rechecked one narrow question only: does `http://127.0.0.1:5174/debug` still claim the city is offline/empty while the live controller shows active residents?

## Orientation Summary

- Dashboard repo branch: `codex/storyteller-overview-bff`
- Dashboard repo dirty state before packet: modified `packages/server/src/projector-overview.ts`, modified `packages/server/src/projector-overview.test.ts`, untracked `docs/qa/`
- Server repo branch: `agents/wip` tracking `origin/agents/wip`, behind upstream, dirty in `docs/agent-status.md`, `docs/issue-register.md`, and existing QA artifacts
- Shared runtime screens present: `nullcity-infra`, `nullcity-game`, `nullcity-controller`, `nullcity-dashboard-server`, `nullcity-dashboard-web`
- Listening ports present: `43591`, `43592`, `43594`, `43595`, `43596`, `43610`, `43611`, `8787`, `5174`
- `qmd` unavailable in `PATH`
- Shell `curl` probes to localhost are blocked in this sandbox with `Operation not permitted`, so browser verification was used for HTTP-visible truth

## Commands And Probes

- `screen -ls`
- `lsof -nP -iTCP:<port> -sTCP:LISTEN`
- `cd /Users/james/Code/OnionDAO/rs6-nullcity-server && npm run controller:smoke -- --observe-seconds 15 --allow-recent-visible`
- `cd /Users/james/Code/OnionDAO/rs6-nullcity-server && npm run controller:status`
- In-app browser, fresh tab to `http://127.0.0.1:5174/debug`

## Runtime Truth For Correlation

### 1. Current live smoke shows an active 10-resident cohort

Command:

```bash
cd /Users/james/Code/OnionDAO/rs6-nullcity-server && npm run controller:smoke -- --observe-seconds 15 --allow-recent-visible
```

Observed:

- `memory=/Users/james/Code/OnionDAO/rs6-nullcity-server/data/controller/memory residents=10`
- `8 OK`, `2 WARN`
- WARN rows were limited to:
  - `res:qa-trader` with `observed_no_action_decision_loop:follow_listen_hold`
  - `res:qa-social` with `observed_no_action_decision_loop:follow_listen_hold`
- Active cohort observed in this same run:
  - `res:agent`
  - `res:hans`
  - `res:qa-woodcutter`
  - `res:qa-cook`
  - `res:qa-survivor`
  - `res:qa-guardian`
  - `res:qa-trader`
  - `res:qa-banker`
  - `res:qa-social`
  - `res:qa-scout`

### 2. Current controller status still reports a live city, not an empty one

Command:

```bash
cd /Users/james/Code/OnionDAO/rs6-nullcity-server && npm run controller:status
```

Observed at `2026-06-04T18:24:49.315Z`:

- `HEALTH: ok — probe ok (4870ms)`
- `residents=25 erroring=0`
- `ALIVE_ACTING` residents included:
  - `res:agent`
  - `res:qa-banker`
  - `res:qa-cook`
  - `res:qa-guardian`
  - `res:qa-scout`
  - `res:qa-survivor`
  - `res:qa-trader`
  - `res:qa-woodcutter`
- Additional runtime state was present for `res:hans` and `res:qa-social` as `STUCK`, which is still incompatible with a truthful `ONLINE 0 / RUNTIME 0` dashboard claim

## Browser Evidence

### Fresh `/debug` load stayed false-red through ten seconds

- Route: `http://127.0.0.1:5174/debug`
- Method: in-app browser, fresh tab, read-only
- Screenshot artifacts:
  - `/private/tmp/qa-20260604-1322-debug-overview-truth/t0.png`
  - `/private/tmp/qa-20260604-1322-debug-overview-truth/t3.png`
  - `/private/tmp/qa-20260604-1322-debug-overview-truth/t10.png`

Observed at `t0`, `t+3s`, and `t+10s` with no interaction besides waiting:

- `Loading dashboard state`
- `GATEWAY offline`
- `CONTROLLER quiet`
- `ONLINE 0`
- `RUNTIME 0`
- `No readiness checks reported`
- `No residents match the current filters`
- `No recent events`
- `No recent patron letters`

The same false state persisted across all three captures; this run did not self-correct by `+10s`.

## Expected Versus Observed

- Expected: `/debug` should not claim `offline / quiet / 0 / 0` while controller tools show an active cohort and fresh resident actions
- Observed: `/debug` remained in a false-empty state for the entire 10-second check window despite same-run controller evidence of active residents

## Assessment

Status: `Reproduced`

This is a human-visible truth regression on the operator surface. The live runtime was active in the same packet, but `/debug` still presented a dead city for at least ten seconds. That is worse than the earlier 3-second false-first-paint packet and remains demo-risky.

## Issue Handling

- Canonical issue target remains previously proposed `QA-20260603-091`
- I did not edit `/Users/james/Code/OnionDAO/rs6-nullcity-server/docs/issue-register.md`
- Reason 1: the file is already dirty in another shared thread, so it is a collision zone
- Reason 2: this automation sandbox can write the dashboard repo but not the server repo
- This packet is therefore an evidence-only handoff

## QA Result

- Result: regression reproduced
- Mutations performed: none
- Runtime restart requested/performed: none
- Code fix attempted: none

## Recommended Next Narrow Target

Keep the next cycle on the same surface but verify one layer deeper:

1. compare `/debug` browser state against `/api/overview` through the in-app browser in the same session
2. determine whether the stale false state is frontend hydration, BFF fetch failure handling, or a stuck cached client state
3. avoid broadening into attendee `/` or other dashboard routes until `/debug` truth is isolated
