# QA Packet `qa-20260604-1350-debug-recovery-truth`

- Time: 2026-06-04 13:49-13:52 CDT
- Classification: READ-ONLY
- Scope: Transient runtime recovery truth on attendee `/` versus operator `/debug`
- Dashboard repo SHA: `13d217c`
- Server repo SHA: `53b7761d`
- Runtime authority: shared/stewarded, no restart authority

## Why This Packet

Earlier same-day packets proved two different states:

- `qa-20260604-1319-runtime-availability-readtruth` found the shared stack dark
- `qa-20260604-1320-runtime-active-resident-truth` then found it live again

This cycle stayed narrow on one question only: after a transient controller/BFF loss and recovery inside the same run, do the human-visible dashboard routes recover truthfully?

## Orientation Summary

- Dashboard repo branch: `codex/storyteller-overview-bff`
- Dashboard repo dirty state before packet: modified `packages/server/src/projector-overview.ts`, modified `packages/server/src/projector-overview.test.ts`, untracked `docs/qa/`
- Server repo branch: `agents/wip`, behind upstream, dirty in `docs/agent-status.md`, `docs/issue-register.md`, and existing QA artifacts
- `qmd` unavailable in `PATH`
- Runtime state changed during this packet:
  - initial read: no `nullcity-controller` screen, no `43596/43611` listeners, `controller:status` showed all 25 residents offline
  - later read: expected screens/listeners returned, controller health recovered, active cohort visible again

## Commands And Probes

- `screen -ls`
- `lsof -nP -iTCP:<port> -sTCP:LISTEN`
- `cd /Users/james/Code/OnionDAO/rs6-nullcity-server && npm run controller:status`
- `curl -fsS http://127.0.0.1:8787/api/health`
- `curl -fsS http://127.0.0.1:43596/v1/health`
- `curl -fsS -H 'Authorization: Bearer operator-token' http://127.0.0.1:43611/api/nullcity/economy/heartbeat`
- In-app browser, fresh loads of:
  - `http://127.0.0.1:5174/`
  - `http://127.0.0.1:5174/debug`

## Runtime Truth For Correlation

### 1. The stack was genuinely unstable inside this packet

Initial shell reads around `13:49 CDT` showed:

- `screen -ls`: `nullcity-controller` absent
- listeners present only for `43591`, `43592`, `43594`, `43595`, `5174`, `8787`
- `curl -fsS http://127.0.0.1:43596/v1/health`: connect failure
- `curl -fsS -H 'Authorization: Bearer operator-token' http://127.0.0.1:43611/api/nullcity/economy/heartbeat`: connect failure
- `npm run controller:status`: `HEALTH: UNKNOWN`, all 25 residents `OFFLINE`

Later reads around `13:50-13:51 CDT` showed recovery:

- `screen -ls`: `nullcity-infra`, `nullcity-game`, `nullcity-controller`, `nullcity-dashboard-server`, `nullcity-dashboard-web`
- listeners present again for `43591`, `43592`, `43594`, `43595`, `43596`, `43610`, `43611`, `8787`, `5174`
- `curl -fsS http://127.0.0.1:43596/v1/health`: `{"ok":true,"controller":"ok",...}`
- `curl -fsS -H 'Authorization: Bearer operator-token' http://127.0.0.1:43611/api/nullcity/economy/heartbeat`: `activeResidentCount:10`
- `npm run controller:status`: `HEALTH: ok`, with live cohort members including:
  - `res:qa-banker`
  - `res:qa-scout`
  - `res:qa-social`
  - `res:qa-survivor`
  - `res:qa-trader`
  - `res:qa-woodcutter`
  - plus `STUCK` runtime state for `res:agent`, `res:hans`, `res:qa-cook`, `res:qa-guardian`

## Browser Evidence

### 1. During the dark window, both routes told a dead-city story

Fresh in-app browser loads at roughly `13:49 CDT` showed:

- `/`
  - `Loading city state`
  - `0 / 0 online`
  - `No resident roster loaded yet`
  - `No Storyteller run is loaded yet`
- `/debug`
  - `Loading dashboard state`
  - `Gateway offline`
  - `Controller quiet`
  - `Online 0`
  - `Runtime 0`
  - `No residents match the current filters`

That matched the genuinely dark controller/runtime state at that moment.

### 2. After runtime recovery, attendee `/` recovered truth but `/debug` did not

Fresh reload of `/` after the recovered controller/API reads showed:

- `City overview unavailable; showing live resident fallback.`
- `10 / 23 active`
- `Dispatch is grounded and ready for public review.`
- resident triage populated with offline, missing-plan, and goal-link counts

Fresh reload of `/debug` in the same recovered window still showed:

- `Loading dashboard state`
- `Gateway offline`
- `Controller quiet`
- `Online 0`
- `Runtime 0`
- `No readiness checks reported`
- `No residents match the current filters`
- `No recent events`

## Expected Versus Observed

- Expected after recovery: both `/` and `/debug` should reflect the restored controller/API truth, or at minimum fail consistently with an explicit unavailable state
- Observed after recovery:
  - `/` degraded into a truthful fallback and showed `10 / 23 active`
  - `/debug` stayed false-red at `offline / quiet / 0 / 0`

## Assessment

Status: `Reproduced`

This is not just a first-paint problem. In the same recovered window, the attendee shell had enough live data to present a truthful fallback while the operator `/debug` route remained stuck on an empty-city state. That makes `/debug` the less honest surface after transient runtime loss.

## Issue Handling

- Canonical issue target remains previously proposed `QA-20260603-091`
- I did not edit `/Users/james/Code/OnionDAO/rs6-nullcity-server/docs/issue-register.md`
- Reason 1: the file is already dirty in another shared thread
- Reason 2: this cycle stayed evidence-only and inside the dashboard repo write boundary

## QA Result

- Result: regression reproduced
- Mutations performed: docs-only evidence note in dashboard repo
- Runtime restart requested/performed: none
- Code fix attempted: none

## Recommended Next Narrow Target

Keep the next cycle on `/debug` only:

1. compare its fetch path with the attendee `/` fallback path in the same recovered session
2. determine whether `/debug` is failing hydration, failing its BFF request, or pinning stale client state after an outage
3. avoid broader dashboard routes until `/debug` truth after recovery is isolated
