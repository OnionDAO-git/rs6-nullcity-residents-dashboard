# QA Packet `qa-20260604-1325-runtime-smoke-cohort`

- Time: 2026-06-04 13:22:51 CDT
- Classification: READ-ONLY
- Scope: shared runtime availability and live 10-resident smoke truth
- Repos inspected: `rs6-nullcity-server` (read-only here), `rs6-nullcity-residents-dashboard`

## Orientation summary

- Dashboard repo branch: `codex/storyteller-overview-bff` tracking `origin/codex/storyteller-overview-bff`; untracked `docs/qa/`.
- Server repo branch: `agents/wip` tracking `origin/agents/wip` and `behind 57`; dirty `docs/agent-status.md`, `docs/issue-register.md`, plus multiple untracked QA/doc artifacts.
- Shared `screen` sessions present: `nullcity-infra`, `nullcity-game`, `nullcity-controller`, `nullcity-dashboard-server`, `nullcity-dashboard-web`, `nullcity-storyteller`.
- Listening ports observed: `127.0.0.1:5174` (`node`), `127.0.0.1:8787` (`bun`).
- `qmd` command: not found in PATH.

## Read-only probes run

1. `npm run controller:status`
2. `npm run controller:smoke -- --observe-seconds 20 --allow-recent-visible`
3. `curl -i --max-time 5 http://127.0.0.1:8787/api/health`
4. `curl -i --max-time 5 http://127.0.0.1:43596/v1/health`
5. `curl -i --max-time 5 http://127.0.0.1:43611/api/nullcity/health`
6. `curl -i --max-time 5 http://127.0.0.1:43596/v1/wall/snapshot`
7. `curl -i --max-time 5 'http://127.0.0.1:43596/v1/inbox?human=demo@onion'`

## Current live truth

### Active residents

`controller:status` reported `residents=25 erroring=0` with 10 live residents:

- `res:agent`
- `res:hans`
- `res:qa-banker`
- `res:qa-cook`
- `res:qa-guardian`
- `res:qa-scout`
- `res:qa-social`
- `res:qa-survivor`
- `res:qa-trader`
- `res:qa-woodcutter`

### Smoke result

`controller:smoke` used the live 10-resident cohort and returned:

- `OK`: `res:agent`, `res:hans`, `res:qa-woodcutter`, `res:qa-cook`, `res:qa-survivor`, `res:qa-trader`, `res:qa-banker`, `res:qa-scout`
- `WARN`: `res:qa-guardian`, `res:qa-social`

Observed warning details:

- `res:qa-guardian`: `issues=effect_timeouts:1,after_submit_interruptions:1,observed_after_submit_interruptions:1`
- `res:qa-social`: `issues=observed_no_action_decision_loop:follow_listen_hold`

Notable positive evidence:

- `res:qa-trader` stayed live and visible with repeated follow/listen hold status speech rather than reading as dead.
- `res:agent` and `res:hans` both produced successful result activity inside the 20s window.
- No residents were `ERRORING` in `controller:status`.

### HTTP/API reachability from this sandbox

All localhost `curl` probes failed with immediate connect errors even while listeners were visible via `lsof`:

- Dashboard BFF `:8787`: unreachable from this environment
- Controller letters/wall/inbox `:43596`: unreachable from this environment
- City API `:43611`: unreachable from this environment

Interpretation: runtime listeners exist, but this sandbox cannot directly verify HTTP response truth. That blocks human-visible dashboard/API claims for this packet.

## Cycle outcome

- Proposed target: runtime smoke truth for the active 10-resident cohort
- Safe to proceed: yes, READ-ONLY only
- Evidence status: live runtime health verified through controller CLI; dashboard/controller/wall/inbox/City API response truth unverified due sandbox localhost isolation
- Issue update status: handoff only; this environment cannot edit the server repo issue register

## Recommended next narrow target

Use a browser-capable or unsandboxed localhost path to verify one human-visible surface only:

- first choice: dashboard `/debug` overview resident counts vs `controller:status`
- second choice: wall/inbox read paths on `:43596`
