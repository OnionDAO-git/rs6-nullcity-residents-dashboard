# QA Packet `qa-20260603-2230-debug-wall-truth`

- Time: 2026-06-03 22:30 CDT
- Classification: READ-ONLY
- Dashboard repo SHA: `2b5190e`
- Server repo SHA: `53b7761d`
- Runtime authority: shared/stewarded, no restart authority

## Scope

Verify one narrow human-visible truth path: whether the live legacy Embassy wall under `/debug/wall` reflects the current live city, or still presents stale death/broadcast history as current.

## Orientation

- Server repo: `agents/wip` tracking `origin/agents/wip`, behind `35`, dirty in shared docs (`docs/agent-status.md`, `docs/issue-register.md`) plus existing QA artifacts.
- Dashboard repo: `codex/storyteller-overview-bff` tracking `origin/codex/storyteller-overview-bff`, untracked `docs/qa/` only.
- Shared screens present: `nullcity-infra`, `nullcity-storyteller`, `nullcity-dashboard-server`, `nullcity-game`, `nullcity-controller`, `nullcity-dashboard-web`.
- Expected dashboard listeners present: `127.0.0.1:8787` and `127.0.0.1:5174`.
- `qmd` is not installed on `PATH`.
- Shell and local Node localhost probes are sandbox-blocked (`connect EPERM`), so live verification used controller CLI plus in-app browser only.

## Current active residents

Read-only `npm run controller:status` at `2026-06-04T03:28:35.074Z` reported:

- `HEALTH: ok`
- `residents=25 erroring=0`
- live acting cohort: `res:qa-banker`, `res:qa-cook`, `res:qa-guardian`, `res:qa-scout`, `res:qa-social`, `res:qa-survivor`, `res:qa-woodcutter`
- currently stuck but still live: `res:agent`, `res:hans`, `res:qa-trader`

## Evidence

### 1. `/debug` shows a live city

Route: `http://127.0.0.1:5174/debug`

Observed visible state:

- `GATEWAY connected`
- `CONTROLLER available`
- `ONLINE 10`
- `RUNTIME 27`
- `EVENT READINESS READY`

This matches the controller CLI's live 10-resident cohort and establishes that the operator shell itself is not globally dead.

### 2. `/debug/wall` still shows stale death broadcasts

Route: `http://127.0.0.1:5174/debug/wall`

Observed visible state includes:

- eight repeated `[Broadcast] On the passing of res:loop-check` cards, all stamped `01:47 PM`
- multiple old resident cards such as `res:mother-anvil`, `res:severn-vesta`, `res:the-hush`, `res:wf-verify-born`, `res:restart-test`, `res:wise-old-man`
- footer text `0 deaths today as of 10:30 PM`

This is not consistent with the current live controller cohort and reproduces the earlier wall-truth regression on the current stack.

### 3. The wall is stale while the city is live

Same-run correlation:

- controller CLI says the live cohort is 10 residents, with `res:qa-banker`, `res:qa-cook`, `res:qa-guardian`, `res:qa-scout`, `res:qa-social`, `res:qa-survivor`, and `res:qa-woodcutter` acting now
- `/debug` presents that live city as connected and available
- `/debug/wall` continues to foreground duplicate `res:loop-check` death broadcasts and old residents instead of current live truth

## Assessment

Status: `Reproduced`

The narrow wall truth bug is still present on the shared stack. This is human-visible and demo-relevant, but the packet remained read-only and did not require runtime mutation.

## Recommended next narrow target

`/debug/inbox?human=demo@onion` stale correspondence truth, reusing this same wall/legacy read-model slice. That stays read-only and can determine whether the stale data is limited to wall cards or shared across Embassy read surfaces.
